{
  "name": "osasg-ai",
  "version": "0.0.0",
  "description": "Collection of bots for OSASG",
  "main": "js/index.js",
  "scripts": {
    "clean": "rm -rf js",
    "build": "npm run clean && tsc",
    "start": "npm run build && node js/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David McLaughlin",
  "license": "Unlicense",
  "dependencies": {
    "carrier": "^0.3.0",
    "ts-turnbased": "0.0.2",
    "ts-turnbased-connect": "0.0.5",
    "typescript": "^2.2.2"
  },
  "devDependencies": {
    "@types/node": "^7.0.12"
  }
}
